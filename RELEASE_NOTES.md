# v1.6.3 发布说明

`IdcsmartClientLevel 1.6.3` 是面向 ZJMF-CBAP V10 的用户等级、双线贡献与推广权益插件。

作者与维护者：`006IDC`。

## 下载与安装

请从本次 Release 的 Assets 下载：

- `idcsmart_client_level_v1.6.3_zjmf_v10_import.zip`：V10 后台直接导入的正式安装包；
- `SHA256SUMS.txt`：安装包和源码归档的 SHA-256 校验值。

不要使用 GitHub 自动生成的 Source code 压缩包作为 V10 安装包。

在 V10 后台上传正式安装包，完成“安装”和“启用”。也可以将 ZIP 解压到：

```text
<V10根目录>/public/plugins/addon/
```

最终入口应位于：

```text
<V10根目录>/public/plugins/addon/idcsmart_client_level/IdcsmartClientLevel.php
```

直接复制文件后仍需在 V10 后台执行安装和启用。使用推广提现时，还需要安装并启用 V10 提现能力。

## 本版内容

- 继续复用 V10 兼容用户等级、用户等级关联与商品折扣数据结构；
- 本人累计净消费和已锁定推广贡献独立达标，取较高等级，不允许金额相加；
- 推广权益支持在观察期后选择提现或纳入等级贡献；
- 每个商品可以独立加入或移出返利计划；
- 提现统一进入 V10 提现管理，退款会强制回退权益并处理在途申请；
- 管理端保存、刷新、复制、开关、导入和重算均提供处理中及结果反馈；
- 修复设置、等级、商品开关、绑定、收款方式与提现关键状态可能出现的事务分叉；
- 完成公开源码匿名化、构建工具、部署文档、CI、安全政策和开源审计。

## 验证结果

- 普通首装：通过；
- 重复 `upgrade()`：通过；
- V10 相关表：20 张，未修改核心用户、商品和订单表结构；
- 本人消费与等级测试：12/12；
- 双线等级、返利、退款与提现测试：19/19；
- 下单到提现完整生命周期：20/20；
- 邀请链接注册绑定：通过；
- PHP、JavaScript、模板一致性、ZIP 解压与隐私扫描：通过。

运行时验证基线为 ZJMF-CBAP V10 10.4.6、PHP 7.3.33、MySQL 5.7。其他 V10 版本应在上线前重新执行安装、升级、支付、退款与提现测试。

## 升级提示

请先备份数据库和原插件目录，再使用保持相同插件身份的覆盖升级流程。不要通过卸载再安装进行常规升级；卸载会改变权限和导航状态。

更完整的安装、升级与排错说明见 README 和 `docs/DEPLOYMENT.md`。
