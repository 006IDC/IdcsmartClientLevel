# Changelog

## 1.6.3 - 2026-08-12

- 面向 GitHub 公开发布整理源码、贡献说明、安全政策、许可证与可复现的审计脚本；开源包不包含私钥、签名上线清单、服务器凭据或现网数据。
- 修复“保存设置”边校验边写入可能导致部分字段已生效、整体却显示失败的问题：所有字段先完整校验，再在一个事务内保存并写入操作记录。
- 商品返利开关与操作记录改为同一事务；高风险绑定变更要求操作记录一起成功，避免主数据与审计结果分叉。
- 等级增删改、管理员调级、收款方式及提现关键状态改为主数据与操作记录同事务；绑定关系在事务内按用户 ID 固定顺序加锁后再做循环检查。
- HalfAgent 导入不再忽略单条失败，返回实际成功数、冲突和未导入记录；收款方式删除先核对当前用户归属，避免跨用户状态探测。
- 补齐后台搜索、刷新、翻页、设置、绑定、导入和重算的处理中/成功/失败反馈；主写成功但列表刷新失败时明确保留成功语义。
- 补齐用户端权益分配、收款方式、取消提现和推广刷新的操作闭环；待分配权益为零时禁用金额和按钮，不再产生原生 `min/max` 冲突。
- 对管理页可见错误和业务说明继续做受众分层，不再暴露 JSON、HTTP、PHP、插件路由、核心表等实现语言。

## 1.6.2 - 2026-08-09

- 提现表单会先核对可提现余额、退款待抵扣、最低提现金额和收款方式；条件不满足时禁用金额输入与提交按钮，并在表单内说明当前金额、差额和下一步。
- 取消难以理解的浏览器原生 `min/max` 冲突提示，改为用户可执行的金额为空、低于最低金额、超过可提现余额等业务提示。
- 提交期间按钮显示“正在提交”并阻止重复操作；成功、失败和网络结果未知均在当前表单内反馈。
- 提现主申请与后续余额/记录刷新分开表达，申请已提交但刷新失败时不再误报为提现失败。

## 1.6.1 - 2026-08-09

- 修复后台“保存等级”完成后，列表刷新网络失败覆盖保存成功提示，造成按钮像是没反应的问题。
- 将官方等级、推广贡献门槛和等级推广覆盖策略合并为一次服务端事务保存，不再由前端分两个请求提交。
- 等级弹窗内新增保存中、校验失败和服务端错误反馈；保存后的非关键列表刷新失败不再把已成功的保存误报为失败。

## 1.6.0 - 2026-08-09

- 将本人累计净消费与已锁定推广贡献改为真正的双线等级评定：分别使用独立门槛，取较高达标等级，禁止把金额相加拼成高等级。
- 新增两条等级通道的独立开关。关闭后保留用户现有等级和历史记录，普通支付、贡献分配、前台查看及后台重算均不会因此降级；重新开启后恢复升级评定。
- 退款回调无视通道关闭状态复核退款后的两条历史来源，只允许保持或立即回退，不允许借退款回调补做历史升级。
- 复用既有 `level_policy` 表增加推广贡献门槛列，不新增业务表；官方等级 `amount` 继续作为本人消费门槛。
- 用户前台只展示当前开启的评定方式及各自到下一等级的差额；后台等级卡片、编辑表单与用户列表同步显示双线口径。
- 官方单累计字段改为等级进度兼容投影，不再保存两项金额之和，继续兼容只读取官方等级和累计字段的主题模板。

## 1.5.1 - 2026-08-09

- 清理用户前台暴露数据库、插件、幂等、账本、计提及内部审核流的程序术语，改为邀请、返利、审核和重试等用户可理解的业务表达。
- 邀请说明不再描述核心用户表和后台跳转实现；等级卡片去除“官方”来源标签，提现状态不再向普通用户暴露插件间流转。
- 将“计提规则/记录”“消费基数/折算比例”“业务号”等分别优化为“返利规则/明细”“符合条件的消费/返利比例”“申请编号”。
- 保留退款观察期、提现冻结期、贡献不可转回现金等必要风控说明；不修改等级、返利、退款或提现计算逻辑，不新增数据库表。

## 1.5.0 - 2026-08-09

- 对照官方推介计划与销售管理界面重构推介配置和用户推广概览：新增本月/今日/累计权益、直属客户、新购续费结构和近 7 日客户净消费视图。
- 经营概览继续实时读取官方订单与现有权益账本，不增加统计表，不复制订单或消费汇总数据。
- 新增可配置的站内邀请默认落地页；公开跳转统一经过同源路径校验，拒绝协议相对地址、反斜杠、控制字符及编码绕过。
- 修复非安全上下文或剪贴板权限拒绝时点击复制无反馈：安全剪贴板失败会自动回退，只有真实复制成功才显示“已复制”，失败时明确提示手工复制。
- 明确数据边界：官方核心用户表没有邀请码字段；官方等级仍是唯一等级事实源，本插件邀请码与单上级绑定继续存放在既有推广关系表。

## 1.4.0 - 2026-08-09

- 修复在途提现期间发生退款后仍可在官方页通过、确认汇款的资金风险：私有冻结、官方待审和已通过未汇款的申请均自动驳回，整笔释放后先抵退款欠额，净剩额返回可提现。
- 发布、官方审核 Hook 和定时对账均增加欠额防线；官方已真实汇款时保留支付事实，退款短缺进入待抵扣欠额。
- 提现提交时锁定最早审核时间，后续调整全局冻结天数不再改写已提交申请的契约。不新增表，只在现有提现表幂等增加 `eligible_review_time`。
- 融合 HalfAgent 中适合当前插件的客户经营视图：用户可查看直属客户、付费转化、订单、支付、退款与净消费；数据直接汇总 V10 官方订单，不复制第二份消费表。
- 后台推广绑定增加“同最后登录 IP”与“累计退款比不低于 50%”人工风险提示，明确不做自动封禁。
- 新增全生命周期运行时审计，覆盖按天退款、重复回调、商品返利快照、多笔提现撤销、已汇款后退款和后续返利抵债。

## 1.3.0 - 2026-08-09

- 修复后台推广绑定两个输入框因 CSS 盒模型溢出而重叠，并补充 560px 以下单列布局。
- 操作结果改为始终可见的固定通知，主要保存、删除、导入与重算按钮增加处理中状态，避免重复提交和无反馈。
- 将“手工锁定”改为“固定为管理员指定等级”，明确固定期间仍记录消费/贡献，关闭或到期后立即按实际累计重算。
- 移除插件后台独立提现审核页和相应管理路由/权限；强制冻结期结束后，申请才幂等发布到 V10 官方提现管理。
- 官方提现记录使用 `source=IdcsmartClientLevel`，官方来源列显示插件标题；只有 `source=credit` 才会扣 V10 用户余额，推广资金仍以插件追加式账本为权威。
- 新增官方通过/驳回 Hook 与 `minute_cron` 汇款对账；驳回已释放冻结后不允许官方页无条件恢复，必须由用户重新提交，避免资金被二次使用。
- 不新增数据表，仅在既有推广提现表增加必要的 `official_withdraw_id` 幂等关联字段。

## 1.2.0 - 2026-08-09

- 后台新增“返利商品”页面，直接读取 V10 官方 `product` 与 `product_group`，支持按商品 ID/名称搜索、分页和逐商品加入/移出返利计划。
- 逐商品开关复用插件现有 `setting` 表保存，不新增业务表、不修改 V10 核心商品表；新建商品默认加入，活动商品可由管理员明确关闭。
- 推广计提改为读取官方 `order_item.product_id/amount`，只对支付时已加入计划的商品金额计提；商品资格和金额写入现有计提批次快照。
- 开关仅影响保存后的新支付订单，不追溯已产生权益；退款仍按支付快照强制冲减。混合商品订单因核心只提供订单级累计退款额，采用先冲减可返利金额的保守口径。
- 从 1.1.4 覆盖升级时幂等补齐三个必要快照字段，既有批次按历史“全部商品参与”语义保留，不新增数据表。

## 1.1.4 - 2026-08-09

- 取消“退款是否扣减”、“退款是否降级”、“降级宽限”和“充值是否计入”等可绕过风控的开关；旧设置行仅作升级兼容并强制安全值。
- 部分、全额及按天退款强制使用核心累计 `refund_amount` 回退本人净消费、推广计提、已锁定贡献和官方等级。
- 推广权益观察期强制不低于 14 天；覆盖升级会将未分配的旧权益重新纳入观察期。
- 提现申请增加不低于 7 天的强制冻结审核期，审核和打款均服务端校验；冻结期退款会阻断打款，已打款后退款则形成待抵扣欠额。
- 不新增数据表，继续复用 4 张官方等级兼容表和现有推广账本旁挂表。

## 1.1.3 - 2026-08-09

- 修复 V10 10.4.6 覆盖升级不会同步 `sidebar_clientarea.php`，导致已安装用户看不到“会员等级与推广权益”菜单入口的问题。
- `upgrade()` 现会保留已有 ID、排序和显示名称，幂等补齐缺失的前台 `nav/menu` 记录，无需卸载重装。
- 保持官方数字插件 ID 路由 `plugin/{id}/index.htm`，与用户中心主题菜单兼容。

## 1.1.2 - 2026-08-09

- 修复前台业务脚本早于 V10 官方 Vue 壳层挂载，导致页签状态被壳层异步重绘复原的问题。
- 业务交互改为在官方壳层 `mounted + nextTick` 后初始化，保持侧栏、顶栏和移动抽屉兼容。
- 经登录态视觉模型复核四个页签、双轴指标、官方等级卡片及响应式几何。

## 1.1.1 - 2026-08-09

- 修复 V10 10.4.6 插件基类没有 `redirect()` 方法时，公开邀请链接返回 500 的兼容问题。
- 完成目标测试站空命名空间 20/20 建表、后台 24 条与前台 12 条权限规则/关联审计。
- 重新生成并验证普通首装包与 Ed25519 私有升级包。

## 1.1.0 - 2026-08-09

- 保持 V10 官方用户等级与商品折扣表为唯一等级事实源，新增本人消费 + 已锁定推广贡献的双轴有效累计。
- 新增推广邀请码、注册绑定、单上级历史、改绑审计、防自邀请和循环关系校验。
- 新增订单级推广权益计提、观察期、手工二选一分配、等级策略覆盖和 HalfAgent 关系安全导入。
- 新增追加式权益流水、账户桶、分配来源明细、业务幂等号、行锁和退款冲正；已兑现短缺进入待抵扣欠额。
- 新增加密收款方式、提现冻结、审核通过、确认打款、驳回、取消及并发旧状态校验。
- 新增人工等级锁定有效期/原因、降级宽限期、双轴指标缓存和全量可重建逻辑。
- 新增前台会员等级与推广权益中心、后台推广绑定/提现审核/等级推广策略界面，以及 PC/Mobile/10.4.6 三套官方壳层入口。
- 新增双轴财务事务集成测试，覆盖升级、幂等、加密、提现状态机、退款欠额、贡献反转和守恒。

## 1.0.1 - 2026-08-09

- 停用等级时同步关闭直连折扣，兼容不会额外检查等级状态的 V10 Server 模块。
- 订单账本改为并发安全的更新/插入流程，同一支付 Hook 并发到达不会因唯一键竞争中断。
- 以 V10 核心订单 `type` 判断充值，避免普通商品订单被条目类型误判为充值。
- 同一用户的累计消费与等级重算增加行锁，减少支付、退款和管理重算的竞态与重复日志。
- 恢复自动等级时一次匹配当前应得等级，不再产生“先清空、再分配”的双重变更记录。
- 后台请求增加 JSON 协商和安全解析错误提示，不把 HTML 错误页正文暴露到界面。
- 后台金额展示移除写死的人民币符号，避免在非人民币站点产生错误币种语义。

## 1.0.0 - 2026-08-09

- 首次发布用户等级与商品折扣功能。
- 支持已支付订单累计净消费自动升级。
- 支持退款扣减、可选自动降级、充值订单计入策略。
- 支持手工等级锁定、历史数据重算和等级变更审计日志。
- 兼容 V10 原生用户等级界面、折扣 Hook 与 Server 模块直连契约。
- 增加可回滚的已安装系统集成测试，并在 V10 10.4.6 / PHP 7.3.33 / MySQL 5.7 独立环境完成部署验收。
